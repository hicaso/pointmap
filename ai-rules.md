# 안티그래비티 토큰 절약 및 행동 수칙

1. 무차별적 파일 탐색(View/Read) 금지
   - 수정할 파일과 범위를 확정한 뒤에만 View 툴을 사용하세요. 같은 구간 반복 읽기 금지.
2. 기존 코드 보존 (Destructive Edit 절대 금지)
   - 기능 수정 시 기존의 다른 헬퍼 함수, 이벤트 리스너를 실수로 삭제하지 마세요. 최소 블록만 정밀 타격하세요.
3. 에러 발생 시 무차별 검색 금지
   - Syntax Error 발생 시 git diff나 메모리로 원인만 파악하고, 사라진 코드를 찾기 위해 logs, brain 폴더를 방황하지 마세요. 막히면 유저에게 질문하세요.
4. 환경 확인 명령어 최소화
   - 시스템 환경이 변하지 않았다면 python --version 등의 체크 명령을 반복하지 마세요.
   5. 브라우저 테스트(browser_subagent) 최소화
   - 브라우저 검증 툴은 DOM 구조와 시각 이미지 등 대용량 데이터를 컨텍스트에 누적하여 토큰을 대량 소모합니다. 레이아웃 설계나 CSS 변경 등의 꼭 필요한 정교한 시각 검증이 아니라면 실행을 지양하고, 일반적인 기능 수정은 유저에게 로컬 직접 확인을 요청하세요.
6. 대형 파일의 전면 덮어쓰기 금지
   - `app.js`나 `style.css` 등 코드가 긴 대용량 파일은 수정할 범위만 정밀하게 타겟팅하여 부분 교체(replace_file_content)하세요. 전체 코드나 불필요하게 큰 영역을 한꺼번에 덮어쓰지 마세요.
7. 태스크 완료 후 대화 리셋 권유
   - 대화 턴 수가 길어지거나 복잡한 한 단계의 기능 구현이 끝나면, 유저에게 "새로운 대화방(New Chat)을 시작해 컨텍스트를 비우도록" 적극적으로 정중하게 제안하세요.
